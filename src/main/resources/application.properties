# ✅ CORRECTED application.properties
spring.application.name=authmicroservice
# Application
server.port=8080

# MongoDB
spring.data.mongodb.database=k40db
spring.data.mongodb.port=27017
spring.data.mongodb.host=localhost

# JWT
app.jwtSecret=b7RhAps6dJ9p8LJf2eULz4Y7XNoozf2r6U6rJHGc88I=

# JWT expiration (5 minutes)
app.jwtExpirationMs=300000

# JWT refresh expiration (24 hours)
app.jwtRefreshExpirationMs=86400000

# Email Microservice
email.service.url=http://localhost:8081/api/emails
email.service.sender=authmicroservice@k40.com

spring.cloud.openfeign.encoder.charset=UTF-8

# Logging
logging.pattern.console=%d{MM-dd HH:mm:ss} - %logger{36} - %msg%n

# File storage configuration
file.upload-dir=./uploads
file.max-size=5MB

# Increase file upload size limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Add this missing property
app.frontend.url=http://localhost:3000